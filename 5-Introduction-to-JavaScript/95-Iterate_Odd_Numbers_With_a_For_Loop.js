// final-expression can determine on which conditions a for loop will trigger
// the for loop does not have to happen every time, for example we can set to only happen every even time

// Example
var ourArray = [];

for (var i = 0; i < 10; i += 2) {
  ourArray.push(i);
}

// Setup
var myArray = [];

// Only change code below this line.

for (var i = 1; i <=9; i +=2) {
  myArray.push(i);
}
